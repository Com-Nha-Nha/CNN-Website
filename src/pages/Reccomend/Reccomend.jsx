import React from 'react';
import SearchBar from '../../components/SearchBar';

const Reccomend = () => {
    const listReccomend = [
        {

        }
    ]
    return (
        <div>
           
        </div>
    );
};

export default Reccomend;